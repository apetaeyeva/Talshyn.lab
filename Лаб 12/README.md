Лабораторная работа №12
Применение объектно-ориентированного подхода
Дисциплина: Языки и технология программирования
Студент: Талшын
Группа: CS-22
📍 Цель работы

Освоить основные принципы объектно-ориентированного программирования (ООП): создание классов, инкапсуляция данных, использование методов доступа, взаимодействие между объектами и сохранение данных в файл.

📍 Краткое содержание

В рамках лабораторной разработано консольное приложение «Система учёта студентов», реализующее объектно-ориентированный подход.

Программа позволяет:

создавать объекты класса Student;

организовывать группу студентов через класс Group;

обновлять средний балл (GPA) с проверкой корректности;

выводить всех студентов;

удалять студента по имени;

выводить студентов с GPA выше заданного порога;

сохранять список студентов в файл students.json.

📍 Используемые классы
Класс Student

Атрибуты (инкапсулированные):

__name — имя студента

__group — учебная группа

__gpa — средний балл

Методы:

__init__() — конструктор

display_info() — вывод информации

update_gpa() — изменение среднего балла

геттеры: get_name(), get_group(), get_gpa()

Класс Group

Атрибуты:

students — список объектов класса Student

Методы:

add_student() — добавление студента

remove_student() — удаление студента по имени

show_all() — вывод всех студентов

get_top_students() — студенты с GPA выше порога

save_to_json() — сохранение данных в students.json

📍 Исходный код программы (main.py)

Основной фрагмент (полная версия — в файле main.py):

if __name__ == "__main__":
    group = Group()

    # Студенты
    s1 = Student("Талшын", "CS-22", 3.7)
    s2 = Student("Акрам", "CS-22", 2.9)
    s3 = Student("Ринат", "IT-11", 3.95)

    group.add_student(s1)
    group.add_student(s2)
    group.add_student(s3)

    print("\nВсе студенты:")
    group.show_all()

    print("\nОбновляем GPA...")
    s2.update_gpa(3.1)

    print("\nТоп студенты:")
    group.get_top_students(3.0)

    print("\nУдаляем одного студента (Ринат)...")
    group.remove_student("Ринат")

    print("\nПосле удаления:")
    group.show_all()

    group.save_to_json()
    print("\nДанные сохранены в students.json")
